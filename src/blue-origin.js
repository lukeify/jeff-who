var replace__blueorigin = function() {
    var copycat = document.querySelector(".content > .white > h3"); // Talk about make it hard to query...
    if (copycat) {
        copycat.innerHTML = "WE'RE LIKE SPACEX, BUT SUBORBITAL";
    }
};

replace__blueorigin();