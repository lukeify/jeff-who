/**
 * On Blue Origin's website, replace the hero text as needed.
 */
var replace__blueorigin = function() {
    var copycat = document.querySelector(".Heading__HeroHome");
    if (copycat) {
        copycat.innerHTML = "WE'RE LIKE SPACEX, BUT SUBORBITAL";
    }
};

replace__blueorigin();